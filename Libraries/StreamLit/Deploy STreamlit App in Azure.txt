python -m venv venv
source venv\Scripts\activate # For Linux venv/bin/activate
pip install streamlit


pip freeze > requirements.txt


#!/bin/bash
streamlit run AutoDetectLangandTranslate.py --server.port=8000 --server.address=0.0.0.0




# install Azure cli

winget install --exact --id Microsoft.AzureCLI --version 2.67.0
az login
az account show



az webapp deployment source config-local-git --name SaiPythonWebApp --resource-group Practice

az webapp deploy --resource-group Practice --name SaiPythonWebApp --src-path AutoDetectLangandTranslate.zip


